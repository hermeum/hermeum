{{- if include "hermeum.hasAgentConfig" . -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hermeum.fullname" . }}-agentconfig
  labels:
    {{- include "hermeum.labels" . | nindent 4 }}
data:
  agent-config.yaml: |
    {{- .Values.agentConfig | toYaml | nindent 4 }}
{{- end -}}